선택자
========엘리먼트 선택자============================================	
		*(모든것), .(클래스), #(아이디), 태그명
		형제, and 자식, 부모, 후손
========속성관련 선택자============================================	
		시작, 끝, 단어, 글자, 아닌것, 같거나 ~-로 시작, 
		속성값이 ~인것, 속성을 가지고 있는 것
========form양식 선택자============================================
		:input
		:button -> button 태그, input type=button
========필터======================================================
		:text:first